[build-system]
requires = ['setuptools>=65']
build-backend = 'setuptools.build_meta'

[tool.pytest.ini_options]
testpaths = ["test"]
addopts = "--cov=ingen --cov-report=term-missing --cov-fail-under=95"
